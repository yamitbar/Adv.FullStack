VITE_API_URL=http://localhost:3000/api

# Google Maps Platform browser API key (Places API + Maps JavaScript API
# must be enabled on the linked Google Cloud project, and this key should
# be restricted by HTTP referrer). See the root README's Google Places
# setup section. Never commit a real key - this is a placeholder only.
VITE_GEOAPIFY_API_KEY=your_geoapify_api_key_here
